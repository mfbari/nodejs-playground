{
  "name": "nodejs-playground",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js",
    "hawk-scan": "docker run -e API_KEY=${HAWK_API_KEY} --rm -v  \"$PWD\":/hawk:rw -it --network host  stackhawk/hawkscan:latest"
  },
  "author": "mfbari",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "ejs": "^3.1.9",
    "express": "^4.18.2"
  }
}
